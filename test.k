variable = 5  // Allow non-strongly typed variables.
print(variable+5)  // Functions
5 > 2 ?  // If statements
while var > 0 // Loops

Scoping is done using ':' and a scope ends with two repeating '\n' characters.